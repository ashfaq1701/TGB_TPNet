# Verified working on Python 3.10 and 3.11 (GPU run on tgbl-wiki).
# numpy must stay <2 (torch 2.0.1 is built against numpy 1.x), and
# py-tgb==2.0.0 + pandas==1.5.3 are required together: py-tgb>=2.2 pulls
# numpy>=2/pandas>=2.2 (breaks torch), and pandas>=2 returns read-only
# arrays that crash tgb's dataset pre-processing.
# scikit-learn capped at 1.7.2: 1.8+/1.9+ require Python>=3.11 and won't
# install on 3.10.
torch==2.0.1
numpy==1.26.4
pandas==1.5.3
py-tgb==2.0.0
scikit-learn==1.7.2
wandb==0.27.1
numba==0.65.1
tqdm==4.68.0
tabulate==0.10.0
